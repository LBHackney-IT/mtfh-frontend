import { ConfirmationRouter } from "@mtfh/common/lib/components"

import App from "./app"

export default function Root() {
  return (
    <ConfirmationRouter>
      <App />
    </ConfirmationRouter>
  )
}
